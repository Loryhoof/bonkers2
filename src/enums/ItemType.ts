// enums.ts

enum ItemType {
    RESOURCES = "resources",
    AMMO = "ammo",
    FIREARM = 'firearm',
    TOOL = 'tool',
    BUILDING = 'building',
    INTERACTABLE = 'interactable'
}

export default ItemType;
